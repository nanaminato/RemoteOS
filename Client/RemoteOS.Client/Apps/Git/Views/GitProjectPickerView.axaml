<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:git="clr-namespace:Client.Apps.Git"
             xmlns:loc="clr-namespace:Client.Localization"
             xmlns:proto="clr-namespace:RemoteOS.Protocol.Git;assembly=RemoteOS.Protocol"
             x:Class="Client.Apps.Git.Views.GitProjectPickerView"
             x:DataType="git:GitClientViewModel">
    <Grid RowDefinitions="Auto,*,Auto">
        <!-- 顶部标题 -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" Padding="24,18,24,12">
            <StackPanel Spacing="4">
                <TextBlock Text="{loc:Loc git.picker.title}" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock Text="{loc:Loc git.picker.description}" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <!-- 主体 -->
        <ScrollViewer Grid.Row="1" Padding="24,12" Background="{DynamicResource AppBackgroundBrush}">
            <StackPanel Spacing="14">
                <!-- 已注册项目列表 -->
                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="12">
                    <StackPanel Spacing="8">
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Grid.Column="0" Text="{loc:Loc git.picker.registered_section}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" />
                            <Button Grid.Column="1" Content="⟳" Command="{Binding RefreshRepositoriesCommand}"
                                    Background="Transparent" Foreground="{DynamicResource TextSecondaryBrush}" BorderThickness="0" Padding="8,2" />
                        </Grid>

                        <ItemsControl ItemsSource="{Binding Repositories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="proto:GitRepositoryDto">
                                    <Button Click="OpenProject_Click" Tag="{Binding}"
                                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                            Background="Transparent" Foreground="{DynamicResource TextSecondaryBrush}" BorderThickness="0"
                                            Padding="10,8" Margin="0,2" CornerRadius="4">
                                        <StackPanel Spacing="2">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <TextBlock Text="📁" FontSize="14" />
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" />
                                            </StackPanel>
                                            <TextBlock Text="{Binding Path}" FontSize="11" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="NoWrap" />
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="{loc:Loc git.picker.empty_hint}"
                                   FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" Margin="4,8"
                                   IsVisible="{Binding Repositories.Count, Converter={x:Static git:GitPickerConverters.IsZeroToVisible}}" />
                    </StackPanel>
                </Border>

                <!-- 探测提示 -->
                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="12"
                        IsVisible="{Binding IsProbing}">
                    <StackPanel Spacing="6">
                        <TextBlock Text="{Binding ProbeHint}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" />
                        <ProgressBar IsIndeterminate="True" Height="4" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部操作按钮 -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" Padding="24,10">
            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                <Button Command="{Binding OpenFolderCommand}"
                        Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextOnAccentBrush}" Padding="14,6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="📂" />
                        <TextBlock Text="{loc:Loc git.picker.open_folder}" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding RegisterRepositoryCommand}"
                        Background="Transparent" Foreground="{DynamicResource TextSecondaryBrush}" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1" Padding="14,6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="✎" />
                        <TextBlock Text="{loc:Loc git.picker.register_path}" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
