<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:Client.Localization"
             xmlns:views="clr-namespace:Client.Apps.WebServers.Views"
             x:Class="Client.Apps.WebServers.Views.ExistingNginxInstallationDialogView"
             x:DataType="views:ExistingNginxInstallationDialogViewModel"
             Background="#FFFFFF"
             Width="530">
    <Grid Margin="20" RowDefinitions="Auto,Auto" RowSpacing="18">
        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock Text="⚠" FontSize="28" Foreground="#C77A00" VerticalAlignment="Top" />
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Foreground="#23324D" MaxWidth="430" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button Content="{loc:Loc common.cancel}" Command="{Binding CancelCommand}" Padding="16,6" />
            <Button Content="{Binding ReuseLabel}" Command="{Binding ReuseCommand}" Padding="16,6" />
            <Button Classes="danger" Content="{Binding ReplaceLabel}" Command="{Binding ReplaceCommand}" Padding="16,6" />
        </StackPanel>
    </Grid>
</UserControl>
