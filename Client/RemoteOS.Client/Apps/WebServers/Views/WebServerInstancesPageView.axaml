<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:Client.Localization"
             xmlns:webservers="clr-namespace:Client.Apps.WebServers"
             x:Class="Client.Apps.WebServers.Views.WebServerInstancesPageView"
             x:DataType="webservers:WebServerManagerViewModel">
    <UserControl.Styles>
        <Style Selector="Border.nginx-running-pulse">
            <Style.Animations>
                <Animation Duration="0:0:1.8" IterationCount="INFINITE">
                    <KeyFrame Cue="0%"><Setter Property="Opacity" Value="0.18" /></KeyFrame>
                    <KeyFrame Cue="50%"><Setter Property="Opacity" Value="0.7" /></KeyFrame>
                    <KeyFrame Cue="100%"><Setter Property="Opacity" Value="0.18" /></KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>
    </UserControl.Styles>
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <Grid MaxWidth="1440" HorizontalAlignment="Stretch" Margin="32,28,32,32" ColumnDefinitions="*,1.35*" ColumnSpacing="18">
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" CornerRadius="14" Padding="24">
                <StackPanel Spacing="14">
                    <StackPanel Spacing="3">
                        <TextBlock Text="{loc:Loc webservers.instances.title}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" />
                        <TextBlock Text="{loc:Loc webservers.instances.subtitle}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="{loc:Loc common.refresh}" Command="{Binding RefreshCommand}" />
                        <Button Content="{loc:Loc webservers.discover}" Command="{Binding DiscoverCommand}" />
                    </StackPanel>
                    <StackPanel Spacing="4">
                        <TextBlock Text="{loc:Loc webservers.instances.discovered}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                        <ComboBox ItemsSource="{Binding Servers}" SelectedItem="{Binding SelectedServer, Mode=TwoWay}" PlaceholderText="{loc:Loc webservers.instances.placeholder}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{Binding ManagementMode, Converter={x:Static webservers:WebServerEnumTextConverter.Instance}, ConverterParameter=management}" />
                                        <TextBlock Text="{Binding ExecutablePath}" TextTrimming="CharacterEllipsis" MaxWidth="420" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                    <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" Foreground="{DynamicResource TextPrimaryBrush}" />
                    <StackPanel IsVisible="{Binding IsManagedInstallAvailable}" Spacing="8">
                        <TextBlock Text="{loc:Loc webservers.managed.installation}" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" />
                        <StackPanel IsVisible="{Binding IsWindowsServer}" Spacing="6">
                            <TextBlock Text="{loc:Loc webservers.managed.installation_help}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                            <ComboBox ItemsSource="{Binding AvailableWindowsVersions}" Text="{Binding InstallVersion, Mode=TwoWay}" IsEditable="True" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Content="{loc:Loc webservers.managed.refresh_versions}" Command="{Binding RefreshWindowsVersionsCommand}" />
                                <Button Content="{loc:Loc webservers.managed.select_package}" Command="{Binding SelectLocalPackageCommand}" />
                            </StackPanel>
                            <TextBlock Text="{Binding LocalPackageName}" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" />
                        </StackPanel>
                        <StackPanel IsVisible="{Binding IsLinuxServer}" Spacing="6">
                            <TextBox PlaceholderText="{loc:Loc webservers.managed.version}" Text="{Binding InstallVersion}" />
                            <TextBlock Text="{loc:Loc webservers.managed.linux_version_hint}" TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                        </StackPanel>
                        <Button Classes="primary" Content="{loc:Loc webservers.managed.install}" Command="{Binding InstallManagedCommand}" HorizontalAlignment="Left" />
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="{DynamicResource SurfaceBrush}" CornerRadius="14" Padding="24">
                <StackPanel Spacing="16">
                    <StackPanel Spacing="3">
                        <TextBlock Text="{loc:Loc webservers.actions.title}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" />
                        <TextBlock Text="{Binding ManagementHint}" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" />
                    </StackPanel>
                    <Border Background="{DynamicResource SurfaceSunkenBrush}" CornerRadius="10" Padding="14,10" IsVisible="{Binding !IsManagedServer}">
                        <StackPanel Spacing="3">
                            <TextBlock Text="{loc:Loc webservers.runtime_status}" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" />
                            <TextBlock Text="{Binding SelectedStatusText}" TextWrapping="Wrap" Foreground="{DynamicResource TextPrimaryBrush}" />
                        </StackPanel>
                    </Border>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                        <Button Grid.Column="0" Content="{loc:Loc webservers.action.test_config}" Command="{Binding TestConfigurationCommand}" HorizontalContentAlignment="Center" />
                        <Button Grid.Column="1" Content="{loc:Loc webservers.action.refresh_status}" Command="{Binding RefreshStatusCommand}" HorizontalContentAlignment="Center" />
                    </Grid>
                    <StackPanel IsVisible="{Binding IsExternalServer}" Spacing="8">
                        <TextBlock Text="{loc:Loc webservers.external.help}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" />
                        <Button Classes="primary" Content="{loc:Loc webservers.action.integrate}" Command="{Binding IntegrateCommand}" HorizontalAlignment="Left" />
                    </StackPanel>
                    <StackPanel IsVisible="{Binding IsIntegratedServer}" Spacing="8">
                        <TextBlock Text="{loc:Loc webservers.integrated.help}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                        <Button Content="{loc:Loc webservers.action.reload}" Command="{Binding ReloadCommand}" HorizontalAlignment="Left" />
                    </StackPanel>
                    <StackPanel IsVisible="{Binding IsIntegratedOrManagedServer}" Spacing="6">
                        <TextBlock Text="{loc:Loc webservers.acme.help}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" />
                        <Button Content="{loc:Loc webservers.action.enable_acme_http01}" Command="{Binding EnableAcmeHttp01Command}" HorizontalAlignment="Left" />
                    </StackPanel>
                    <StackPanel IsVisible="{Binding IsManagedServer}" Spacing="10">
                        <TextBlock Text="{loc:Loc webservers.service_controls}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" />
                        <Border Background="{DynamicResource SurfaceSunkenBrush}" CornerRadius="14" Padding="18">
                            <Grid ColumnDefinitions="126,*,Auto" ColumnSpacing="18">
                                <Button Grid.Column="0" Command="{Binding ToggleManagedCommand}" ToolTip.Tip="{Binding ManagedLifecycleActionText}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Grid Width="92" Height="92">
                                        <Border Classes="nginx-running-pulse" Background="{DynamicResource SuccessMutedBrush}" CornerRadius="46" IsVisible="{Binding IsManagedServerRunning}" />
                                        <Border Background="{DynamicResource WarningMutedBrush}" CornerRadius="46" IsVisible="{Binding !IsManagedServerRunning}" Opacity="0.55" />
                                        <Border Background="{DynamicResource SuccessBrush}" CornerRadius="37" Margin="9" IsVisible="{Binding IsManagedServerRunning}" />
                                        <Border Background="{DynamicResource WarningBrush}" CornerRadius="37" Margin="9" IsVisible="{Binding !IsManagedServerRunning}" />
                                        <TextBlock Text="⏻" FontSize="34" Foreground="{DynamicResource TextOnAccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Grid>
                                </Button>
                                <StackPanel Grid.Column="1" Spacing="5" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Border Width="9" Height="9" CornerRadius="5" Background="{DynamicResource SuccessBrush}" IsVisible="{Binding IsManagedServerRunning}" VerticalAlignment="Center" />
                                        <Border Width="9" Height="9" CornerRadius="5" Background="{DynamicResource WarningBrush}" IsVisible="{Binding !IsManagedServerRunning}" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding ManagedRuntimeStateLabel}" FontSize="18" FontWeight="SemiBold" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding ManagedRuntimeStateDescription}" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" />
                                    <TextBlock Text="{Binding SelectedStatusText}" TextWrapping="Wrap" Foreground="{DynamicResource TextTertiaryBrush}" FontSize="11" />
                                </StackPanel>
                                <Button Grid.Column="2" Classes="primary" Content="{Binding ManagedLifecycleActionText}" Command="{Binding ToggleManagedCommand}" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                            <Button Grid.Column="0" Content="{loc:Loc webservers.action.restart}" Command="{Binding RestartCommand}" HorizontalContentAlignment="Center" />
                            <Button Grid.Column="1" Content="{loc:Loc webservers.action.reload}" Command="{Binding ReloadCommand}" HorizontalContentAlignment="Center" />
                        </Grid>
                        <Border Background="{DynamicResource WarningMutedBrush}" BorderBrush="{DynamicResource DangerBrush}" BorderThickness="1" CornerRadius="10" Padding="12,9">
                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                                <TextBlock Text="{loc:Loc webservers.managed.uninstall_hint}" FontSize="12" Foreground="{DynamicResource DangerBrush}" VerticalAlignment="Center" TextWrapping="Wrap" />
                                <Button Grid.Column="1" Content="{loc:Loc webservers.managed.uninstall}" Command="{Binding UninstallManagedCommand}" />
                            </Grid>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="{Binding TestResultText}" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
