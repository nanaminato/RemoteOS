<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:Client.Localization"
             xmlns:vm="clr-namespace:Client.Apps.TaskManager.ViewModels"
             xmlns:conv="clr-namespace:Client.Apps.TaskManager.Converters"
             xmlns:charts="clr-namespace:Client.Apps.TaskManager.Controls"
             xmlns:dto="clr-namespace:RemoteOS.Protocol.SystemMonitor;assembly=RemoteOS.Protocol"
             x:Class="Client.Apps.TaskManager.Views.TaskManagerMainView"
             x:DataType="vm:TaskManagerViewModel"
             Background="{DynamicResource SurfaceBrush}">

    <UserControl.Resources>
        <ControlTheme x:Key="TabButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="16,7" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
        </ControlTheme>
    </UserControl.Resources>
    <UserControl.Styles>
        <Style Selector="ListBox.performance-nav">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style Selector="ListBox.performance-nav > ListBoxItem">
            <Setter Property="Padding" Value="10,9" />
            <Setter Property="Margin" Value="4,1" />
            <Setter Property="CornerRadius" Value="2" />
        </Style>
        <Style Selector="ListBox.performance-nav > ListBoxItem:selected">
            <Setter Property="Background" Value="#DCECFB" />
        </Style>
        <Style Selector="TextBlock.metric-label">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Opacity" Value="0.67" />
        </Style>
    </UserControl.Styles>

    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Top" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="8,5">
            <Grid ColumnDefinitions="Auto,Auto,*,Auto" VerticalAlignment="Center">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,0">
                    <Button Content="{loc:Loc task_manager.performance}" Theme="{StaticResource TabButton}" Command="{Binding SwitchToPerformanceCommand}" Background="{Binding ActiveTab, Converter={x:Static conv:TabBgConverter.Instance}, ConverterParameter=Performance}" />
                    <Button Content="{loc:Loc task_manager.processes}" Theme="{StaticResource TabButton}" Command="{Binding SwitchToProcessesCommand}" Background="{Binding ActiveTab, Converter={x:Static conv:TabBgConverter.Instance}, ConverterParameter=Processes}" />
                </StackPanel>
                <Button Grid.Column="1" Content="{loc:Loc common.refresh}" Command="{Binding RefreshCommand}" Padding="12,5" ToolTip.Tip="F5" />
                <StackPanel Grid.Column="2" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding ConnectionStatus}" FontSize="12" Opacity="0.8" TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding StatusText}" FontSize="11" Opacity="0.55" TextTrimming="CharacterEllipsis" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid>
            <Grid IsVisible="{Binding ActiveTab, Converter={x:Static conv:TabVisibilityConverter.Instance}, ConverterParameter=Performance}" ColumnDefinitions="236,*">
                <Border Grid.Column="0" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,1,0" Padding="0,8">
                    <ListBox Classes="performance-nav" ItemsSource="{Binding PerformanceItems}" SelectedItem="{Binding SelectedPerformanceItem, Mode=TwoWay}">
                        <ListBox.ItemTemplate>
                            <DataTemplate x:DataType="vm:PerformanceResourceItem">
                                <Grid ColumnDefinitions="16,*,Auto">
                                    <Ellipse Width="12" Height="12" VerticalAlignment="Top" Margin="0,3,7,0"><Ellipse.Fill><SolidColorBrush Color="{Binding AccentColor}" /></Ellipse.Fill></Ellipse>
                                    <StackPanel Grid.Column="1" Spacing="1">
                                        <TextBlock Text="{Binding Title}" FontSize="14" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Metric}" FontSize="11" Opacity="0.82" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding SideDetail}" FontSize="10" Opacity="0.55" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="62" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <ScrollViewer Grid.Column="1">
                    <StackPanel Margin="26,16,24,22" Spacing="7">
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel>
                                <TextBlock Text="{Binding SelectedPerformanceItem.Title}" FontSize="30" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding SelectedPerformanceItem.Subtitle}" FontSize="12" Opacity="0.65" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding SelectedPerformanceItem.Metric}" FontSize="19" VerticalAlignment="Bottom" Margin="12,0,0,2" />
                        </Grid>
                        <Grid ColumnDefinitions="*,Auto" Margin="0,4,0,0">
                            <TextBlock Text="最近 60 秒" Classes="metric-label" />
                            <TextBlock Grid.Column="1" Text="{Binding SelectedPerformanceItem.SideDetail}" Classes="metric-label" />
                        </Grid>
                        <charts:PerformanceLineChart Height="270" Values="{Binding SelectedPerformanceItem.History}" Maximum="{Binding SelectedPerformanceItem.ChartMaximum}" LineColor="{Binding SelectedPerformanceItem.AccentColor}" />
                        <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,10">
                            <TextBlock Text="60 秒" Classes="metric-label" />
                            <TextBlock Grid.Column="1" Text="现在" Classes="metric-label" />
                        </Grid>
                        <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="28">
                            <StackPanel><TextBlock Text="{Binding SelectedPerformanceItem.Detail1Label}" Classes="metric-label" /><TextBlock Text="{Binding SelectedPerformanceItem.Detail1Value}" FontSize="21" /></StackPanel>
                            <StackPanel Grid.Column="1"><TextBlock Text="{Binding SelectedPerformanceItem.Detail2Label}" Classes="metric-label" /><TextBlock Text="{Binding SelectedPerformanceItem.Detail2Value}" FontSize="21" /></StackPanel>
                            <StackPanel Grid.Column="2"><TextBlock Text="{Binding SelectedPerformanceItem.Detail3Label}" Classes="metric-label" /><TextBlock Text="{Binding SelectedPerformanceItem.Detail3Value}" FontSize="16" Margin="0,4,0,0" /></StackPanel>
                            <StackPanel Grid.Column="3"><TextBlock Text="{Binding SelectedPerformanceItem.Detail4Label}" Classes="metric-label" /><TextBlock Text="{Binding SelectedPerformanceItem.Detail4Value}" FontSize="16" Margin="0,4,0,0" /></StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <Grid IsVisible="{Binding ActiveTab, Converter={x:Static conv:TabVisibilityConverter.Instance}, ConverterParameter=Processes}" RowDefinitions="Auto,Auto,*">
                <Border Grid.Row="0" BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="0,0,0,1" Padding="8,6">
                    <Grid ColumnDefinitions="*,Auto,Auto,Auto,Auto">
                        <TextBox x:Name="FilterBox" Text="{Binding ProcessFilter, Mode=TwoWay}" PlaceholderText="{loc:Loc task_manager.filter_placeholder}" KeyDown="FilterBox_KeyDown" />
                        <TextBlock Grid.Column="1" Text="{Binding ProcessTotalCount, StringFormat='{}{0} processes'}" VerticalAlignment="Center" Margin="8,0" FontSize="12" Opacity="0.7" />
                        <CheckBox Grid.Column="2" Content="{loc:Loc task_manager.auto_refresh}" IsChecked="{Binding IsAutoRefresh, Mode=TwoWay}" VerticalAlignment="Center" Margin="8,0" />
                        <Button Grid.Column="3" Content="{loc:Loc common.clear}" Command="{Binding ClearFilterCommand}" Padding="12,6" Margin="6,0,0,0" />
                        <Button Grid.Column="4" Content="{loc:Loc task_manager.end_task}" Command="{Binding KillProcessCommand}" Classes="primary" Padding="16,6" Margin="6,0,0,0" />
                    </Grid>
                </Border>
                <Border Grid.Row="1" Padding="8,4" IsVisible="{Binding KillFeedback, Converter={x:Static conv:StringNonEmptyVisibilityConverter.Instance}}"><TextBlock Text="{Binding KillFeedback}" FontSize="12" TextWrapping="Wrap" /></Border>
                <Border Grid.Row="2"><Grid RowDefinitions="Auto,*"><Border Background="{DynamicResource SurfaceBrush}" Padding="8,6"><Grid ColumnDefinitions="2*,70,80,120,120,60"><TextBlock Text="{loc:Loc common.name}" FontSize="12" FontWeight="SemiBold" /><TextBlock Grid.Column="1" Text="PID" FontSize="12" FontWeight="SemiBold" /><TextBlock Grid.Column="2" Text="CPU" FontSize="12" FontWeight="SemiBold" /><TextBlock Grid.Column="3" Text="Memory" FontSize="12" FontWeight="SemiBold" /><TextBlock Grid.Column="4" Text="User" FontSize="12" FontWeight="SemiBold" /><TextBlock Grid.Column="5" Text="Threads" FontSize="12" FontWeight="SemiBold" /></Grid></Border><ListBox Grid.Row="1" ItemsSource="{Binding FilteredProcesses}" SelectedItem="{Binding SelectedProcess, Mode=TwoWay}"><ListBox.ItemTemplate><DataTemplate x:DataType="dto:ProcessInfoDto"><Grid ColumnDefinitions="2*,70,80,120,120,60" Margin="8,5"><TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" /><TextBlock Grid.Column="1" Text="{Binding Id}" /><TextBlock Grid.Column="2" Text="{Binding CpuPercent, StringFormat='{}{0:0.0}%'}" /><TextBlock Grid.Column="3" Text="{Binding MemoryBytes, Converter={x:Static conv:BytesConverter.Instance}}" /><TextBlock Grid.Column="4" Text="{Binding UserName}" TextTrimming="CharacterEllipsis" /><TextBlock Grid.Column="5" Text="{Binding ThreadCount}" /></Grid></DataTemplate></ListBox.ItemTemplate></ListBox></Grid></Border>
            </Grid>
        </Grid>
    </DockPanel>
</UserControl>
