namespace RemoteOS.Protocol.Hubs;

/// <summary>Guardian 日志 Hub 的 client→server 调用方法。</summary>
public static class GuardianLogsHubMethods
{
    public const string Subscribe = nameof(Subscribe);
    public const string Unsubscribe = nameof(Unsubscribe);
}
